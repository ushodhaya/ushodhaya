#include <stdio.h>

int main() {
    int size, arr[100], i;

    printf("Enter the size of the array: ");
    scanf("%d", &size);

    printf("Enter the elements of the array: ");
    for (i = 0; i < size; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Odd numbers in odd indices: ");
    for (i = 1; i < size; i += 2) {
        if (arr[i] % 2 != 0) {
            printf("%d ", arr[i]);
        }
    }

    printf("\n");

    return 0;
}


Sample output:

Enter the size of the array: 7
Enter the elements of the array: 1 2 3 4 5 6 7
Odd numbers in odd indices: 3 5 7

